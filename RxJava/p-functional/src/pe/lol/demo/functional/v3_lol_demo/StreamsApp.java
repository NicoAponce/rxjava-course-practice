package pe.lol.demo.functional.v3_lol_demo;import java.util.ArrayList;import java.util.List;public class StreamsApp {    private List<String> persons;    private List<String> numbers;    public StreamsApp(){        persons=new ArrayList<>();        persons.add("Nicolas");        persons.add("Carmen");        persons.add("Carlos");        persons.add("Maria");        persons.add("Bertha");        numbers=new ArrayList<>();        numbers.add("1");        numbers.add("2");        numbers.add("3");        numbers.add("4");        numbers.add("5");    }    public void filter(){       persons.stream().filter(x ->x.startsWith("C")).forEach(System.out::println);    }    public void order(){        //persons.stream().sorted().forEach(System.out::println);        persons.stream().sorted((x,y)->y.compareTo(x)).forEach(System.out::println);    }    public void transform(){        persons.stream().map(String::toUpperCase).forEach(System.out::println);        numbers.stream().map(x->Integer.parseInt(x)*10).forEach(System.out::println);    }    public void limit(){        persons.stream().limit(3).forEach(System.out::println);    }    public void count(){        Long x=persons.stream().count();        System.out.println(x);    }    public static void main(String[] args) {        StreamsApp app=new StreamsApp();        //app.filter();        //app.order();        //app.transform();        //app.limit();        app.count();    }}